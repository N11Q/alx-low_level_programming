#include <stdio.h>

/**
 * mul - function that multiplies two integers.
 * @a - int going to be multiplied 
 * @b - int going to be multiplied
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	return (a * b);
}
