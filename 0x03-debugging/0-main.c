#include "main.h"

/**
 * main - tests positive_or_negative() if it gives the correct output when given 0
 * Return: 0
 */

int main(void)
{
	int i;
	
	i = 0;
	positive_or_negative(i);
	
	return (0);
}
